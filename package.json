{
  "name": "kirong-ai",
  "version": "7.5.0",
  "private": true,
  "description": "Kirong AI — production AI workspace: multi-provider chat with memory/projects/files/research, a real agent engine (search, files, projects, images, WhatsApp handoff, isolated code execution via Vercel Sandbox, named sub-agent roles, custom agents) governed by a server-authoritative safety layer (risk-gated approvals with per-project permission overrides, audit log, checkpointing, pause/resume, stale-run recovery, prompt-injection wrapping, step undo), a chat UX with per-message edit/regenerate and pinned/archived/renamed conversations, a live Mission Control view, a Command Palette, and Pro upgrades via M-Pesa STK Push.",
  "type": "module",
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "openai": "^4.67.0",
    "groq-sdk": "^0.7.0",
    "formidable": "^3.5.1",
    "@vercel/blob": "^2.6.1",
    "@vercel/sandbox": "^3.0.1",
    "@huggingface/inference": "^4.13.28",
    "pdf-parse": "^1.1.1",
    "mammoth": "^1.8.0"
  }
}
